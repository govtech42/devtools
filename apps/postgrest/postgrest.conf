# Reference config. The compose service sets these via PGRST_* env (source of
# truth), so values here are documentation, not loaded at runtime.
db-uri    = "postgres://authenticator:<BI_AUTHENTICATOR_PASSWORD>@postgres:5432/reporting"
db-schemas = "reporting"
db-anon-role = "bi_reader"
server-port = 3000
