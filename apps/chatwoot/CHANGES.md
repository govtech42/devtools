# Chatwoot fork — divergences from upstream

Branch `code42` off `upstream`. Log each change: file(s), what, why, upstreamable?

| Date | File(s) | Change | Why | Upstreamable |
|------|---------|--------|-----|--------------|
| 2026-06-25 | deploy compose (ours) | external Postgres `chatwoot` + MinIO storage | shared Postgres + S3 attachments | no (deploy-specific) |

<!-- Add a row per fork code change (Kanban + features). Newest at the bottom. -->
